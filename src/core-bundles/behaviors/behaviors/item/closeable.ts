import {BehaviorDefinition} from '../../../../lib/behaviors/behavior';

/**
 * A behavior that indicates a container has some mechanism to open and close
 * it, rather than having an open top.
 */
export const closeable: BehaviorDefinition = {
    listeners: {},
};

export default closeable;
