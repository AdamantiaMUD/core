import {BehaviorDefinition} from '../../../../lib/behaviors/behavior';

/**
 * A behavior that indicates an item can be equipped, and specifies the possible
 * slots which are valid.
 */
export const equippable: BehaviorDefinition = {
    listeners: {},
};

export default equippable;
