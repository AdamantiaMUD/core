---
body: |
    Type {yellow.bold 'commands'} to see a list of possible commands.

    Type {yellow.bold 'help [command]'} to see more information about that command.

    Type {yellow.bold 'help search [keyword]'} to search the helpfiles by keyword.

    Command usage will show arguments as either [optional] or <required>, e.g., look [thing] or get <item> [container]
related:
  - new
  - commands
