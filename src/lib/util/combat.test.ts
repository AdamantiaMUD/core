import each from 'jest-each';

// import * as Combat from './combat';

describe('combat.ts', () => {
    describe('makeCorpse()', () => {
        const testCases = [
            []
        ];

        each(testCases).it('should make the corpse correctly', (data, expected) => {
            expect(false).toBeTruthy();
        });
    });
});
